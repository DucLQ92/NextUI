# PUAE - Commodore Amiga, brick only.
# Layered on top of default.cfg (minarch reads both), so binds and
# every other line from default.cfg still apply.

# kept from the original Brick preset; layered on top of default.cfg, not replacing it
minarch_cpu_speed = Performance

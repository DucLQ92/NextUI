# 32X - Sega 32X
# NextUI default preset for the picodrive core.
# Layer order: system.cfg, then this file, then whatever the player saves
# from the in-game Options menu. Nothing here is locked, so every line
# below shows up in that menu and can be changed per console or per game.

# the 32X needs the recompiler to stand a chance
picodrive_drc = enabled
picodrive_sound_rate = 44100
picodrive_smstype = Auto
picodrive_smsfm = off
picodrive_smsmapper = Auto
picodrive_ggghost = off

bind Up = UP
bind Down = DOWN
bind Left = LEFT
bind Right = RIGHT
bind Mode = SELECT
bind Start = START
bind A Button = Y
bind B Button = X:B
bind C Button = A
bind X Button = B:L1
bind Y Button = L1:X
bind Z Button = R1
